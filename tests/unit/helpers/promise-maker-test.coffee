`import { promiseMaker } from 'dummy/helpers/promise-maker'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | promise maker'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = promiseMaker 42
  assert.ok result
