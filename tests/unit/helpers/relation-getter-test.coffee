`import { relationGetter } from 'dummy/helpers/relation-getter'`
`import { module, test } from 'qunit'`

module 'Unit | Helper | relation getter'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = relationGetter 42
  assert.ok result
