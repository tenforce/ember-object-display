headings
{{#each model.values as |heading index|}}
    each
    {{heading-display model=heading object=object index=index inheritedHelpers=helpers index=index handleHide="handleHide"}}
    /each
{{/each}}
/headings